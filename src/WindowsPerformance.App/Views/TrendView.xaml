<UserControl x:Class="WindowsPerformance.App.Views.TrendView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,16" />
            <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                <Button Content="5 Min" Command="{Binding SelectRangeCommand}" CommandParameter="FiveMinutes" Margin="0,0,8,0" />
                <Button Content="1 Std" Command="{Binding SelectRangeCommand}" CommandParameter="OneHour" Margin="0,0,8,0" />
                <Button Content="24 Std" Command="{Binding SelectRangeCommand}" CommandParameter="TwentyFourHours" Margin="0,0,8,0" />
                <Button Content="Aktualisieren" Command="{Binding LoadTrendsCommand}" />
            </StackPanel>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="CPU %" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <lvc:CartesianChart Series="{Binding CpuSeries}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" Height="160" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="RAM %" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <lvc:CartesianChart Series="{Binding RamSeries}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" Height="160" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Disk %" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <lvc:CartesianChart Series="{Binding DiskSeries}" XAxes="{Binding XAxes}" YAxes="{Binding YAxes}" Height="160" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Optimierungs-Annotations" FontWeight="SemiBold" Margin="0,0,0,8" />
                    <ItemsControl ItemsSource="{Binding Annotations}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Foreground="{StaticResource TextMutedBrush}" Margin="0,2">
                                    <Run Text="{Binding Timestamp, StringFormat={}{0:g}}" />
                                    <Run Text=" — " />
                                    <Run Text="{Binding Module}" FontWeight="SemiBold" />
                                    <Run Text=": " />
                                    <Run Text="{Binding Label}" />
                                </TextBlock>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource AccentOrangeBrush}" TextWrapping="Wrap" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
